# Python package index (PEP 503) — Roberto Locatelli

Static simple-index serving signed release artifacts hosted on GitHub Releases.

```bash
pip install --extra-index-url https://robertolocatelli81-dev.github.io/pypi/ omega-evidence
```

Every file link carries its `#sha256=` fragment: pip verifies the hash on download.
